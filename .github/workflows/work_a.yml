name: CD

on:
  push:
    branches:
      - branch_a

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2


      - name: Create and push build branch
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.email "atitbimali10@gmail.com"
          git config --global user.name "AtitBimali"
          git fetch origin branch_b # Fetch the latest changes from the remote build branch
          git checkout branch_b
          git add .
          git commit -m "Add build artifacts"
          git push origin build